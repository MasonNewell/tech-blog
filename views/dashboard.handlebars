<div class="d-flex row justify-content-center text-center">
  <h4>My Dashboard </h4>
  {{#each blogs as |blog|}}
    <div class="rounded col-7">
      <div class="card">
        <div class="card-header">{{blog.post_title}}
        </div>
        <div class="card-body">
          <div class="card-title">Posted on
            {{format_date blog.date_created}}
          </div>
          <p class="card-text">
            {{blog.post_contents}}
          </p>
        </div>
      </div>
    </div>
  {{/each}}
</div>